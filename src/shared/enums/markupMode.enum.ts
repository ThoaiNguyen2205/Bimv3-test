export enum MarkupMode {
  arrow = 'arrow',
  rectangle = 'rectangle',
  circle = 'circle',
  text = 'text',
  freehand = 'freehand',
  polyline = 'polyline',
  polycloud = "polycloud",
  cloud = "cloud",
}
